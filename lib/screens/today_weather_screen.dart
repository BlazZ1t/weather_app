import 'package:flutter/material.dart';

class TodayWeatherScreen extends StatelessWidget {
  const TodayWeatherScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text("This is today's weather screen"),
      ),
    );
  }
}